机械臂运动规划全面综述：从经典算法到前沿技术第一部分：运动的语言：基本概念1. 机械臂运动规划导论定义核心问题从根本上说，机械臂运动规划的核心问题是寻找一个有效且连续的配置序列（即路径或轨迹），该序列能够将机器人从一个起始状态移动到一个期望的目标状态，同时不违反任何约束条件，其中最关键的约束是避免与自身或环境中的障碍物发生碰撞 1。这个基本问题可以进一步分解为两个子问题：路径规划（Path Planning）和轨迹规划（Trajectory Planning）。路径规划专注于寻找一条纯粹的几何路径，而轨迹规划则为这条路径赋予时间规律，即定义其速度和加速度曲线 3。在现代机器人技术中的关键作用自主规划运动的能力，是将机械臂从一个简单的遥控设备转变为智能体的关键。这项能力是现代机器人技术的基石，它催生了众多深刻影响生产力和社会的应用 3。工业自动化：在制造业中，运动规划对于焊接、复杂零件组装和码垛等精密任务至关重要。在这些结构化环境中，机器人必须执行精确且可重复的运动 3。医疗健康：在医疗领域，机械臂辅助进行需要高度精确性的外科手术，帮助患者康复，并执行实验室自动化任务 5。服务与协作机器人：随着机器人逐渐进入以人为中心的环境，挑战也随之加剧。协作机器人（Cobots）必须能够安全、可预测地在人类周围规划运动，并适应家庭、仓库和医院等动态、非结构化的环境 3。一个“好”规划的标准运动规划研究的最初焦点仅仅是找到任何可行的解决方案，这一概念被称为“完备性”（Completeness）9。然而，现代应用的要求远不止于此。一个“好”的规划需要通过多个标准来评估：可行性（Feasibility）：规划出的路径必须是无碰撞的，并且要遵守机器人的运动学和动力学约束 2。最优性（Optimality）：规划应在给定成本函数下达到“最优”。这可能意味着最小化路径长度、执行时间或能耗 2。平滑性与可预测性（Smoothness & Predictability）：为了安全可靠地执行，轨迹应该是平滑的（即具有较低的加加速度和加速度）。在工业和协作场景中，可预测性至关重要；相似的规划查询应该产生相似的路径 9。这种从可行性到多目标最优性的演变，清晰地反映了机器人技术领域的成熟过程。早期的研究工作主要关注一个二元问题：“是否存在一条路径？” 9。能够保证在路径存在时找到它的算法被称为“完备的”，但对于复杂机器人来说，这些算法的计算成本往往高得令人望而却步。这一计算瓶颈催生了基于采样的规划方法，它们牺牲了绝对的完备性，以换取概率完备性和实践中的高效率，但其初始解往往不平滑且非最优 11。这些初始解的质量不佳，反过来又催生了对“更好”路径的需求，从而引入了最优性的概念（例如，最短路径），并推动了像 RRT* 这样的算法的发展 12。随着机器人变得更加动态，并以更复杂的方式与世界互动，单一的路径长度度量已不再足够。电机扭矩、能量消耗以及流畅运动所需的平滑度等因素变得至关重要 13。这最终导致了轨迹优化框架（如 CHOMP、STOMP、TrajOpt）的兴起，这些框架通过定义丰富的、包含多个项的成本函数来构建问题。这代表了现代运动规划的观点：它不再仅仅是一个搜索问题，而是一个整体的优化问题 15。这一趋势表明，随着机器人越来越多地在现实世界中承担复杂任务，运动的质量变得与运动的存在本身同等重要。这种转变要求规划器能够处理复杂的、通常是不可微的成本函数和约束。2. 位形空间：规划的舞台直观定义我们不必在三维空间中处理多连杆机器人复杂的几何形状，而是可以对问题进行转换。机器人的**位形（Configuration）**是对机器人上每一点位置的完整描述，通常由其关节角度或位置的向量来定义 17。**位形空间（Configuration Space，简称 C-space）**是包含机器人能够达到的所有可能位形的高维空间 18。抽象的力量通过将整个机器人映射为其 C-space 中的一个点，复杂的运动规划问题被简化为：为一个点找到一条从起始位形 $q_{start}$ 到目标位形 $q_{goal}$ 的路径 17。示例：双连杆平面机械臂一个带有两个旋转关节（如同肩膀和肘部）的简单机械臂拥有两个自由度（Degree of Freedom, DOF）：第一个连杆的角度（$\theta_1$）和第二个连杆的角度（$\theta_2$）20。它的 C-space 可以被可视化为一个二维平面，其中 x 轴代表 $\theta_1$，y 轴代表 $\theta_2$。由于角度是循环的（例如，$0^{\circ}$ 与 $360^{\circ}$ 相同），这个平面实际上是一个环面（Torus），即甜甜圈的表面 20。工作空间 vs. 位形空间区分机器人的工作空间（Workspace）（机器人操作所在的 3D 笛卡尔空间）和其 C-space（其关节位形的抽象空间）至关重要 17。工作空间中的一个点（例如，末端执行器的位置）通常可以由 C-space 中的多个不同点达到（例如，“肘部向上”与“肘部向下”的位形）20。C-space 障碍物（C-obstacles）工作空间中的障碍物也必须被映射到 C-space 中。一个C-space 障碍物是所有位形 $q$ 的集合，在这些位形下，机器人的身体会与工作空间中的障碍物发生碰撞 22。自由空间（$C_{free}$）所有有效的、无碰撞的位形的集合被称为自由空间（Free Space）。现在，运动规划问题被正式定义为：在 $C_{free}$ 中找到一条从 $q_{start}$ 到 $q_{goal}$ 的连续路径 20。对于具有许多自由度的机器人来说，显式地计算 $C_{free}$ 的边界在计算上是不可行的，这也是第二部分中讨论的算法出现的主要动机。3. 世界的表达：环境建模与碰撞检测规划的基础运动规划器的性能取决于其对世界的模型。这个模型，或称为环境表示，是用于感知和推理障碍物的数据结构 2。关键的表示方案点云图（Point Cloud Maps, PCM）：这是从激光雷达（LiDAR）或深度相机等传感器获得的原始表示，由大量的三维点组成。优点：几乎不需要后处理，直接反映了传感器数据。缺点：数据非结构化，如果没有像 KD-tree 这样的辅助数据结构，查询（如碰撞检测）的计算成本会很高 24。占据栅格图（Occupancy Grid Maps, OGM）/ 八叉树地图（OctoMaps）：空间被离散化为栅格（二维）或体素（三维，使用八叉树以提高效率）。每个单元被标记为自由、被占据或未知。优点：查询简单，概率更新可以处理传感器噪声。缺点：内存消耗随着分辨率和环境尺寸的增加而急剧增长。由于离散化，可能会丢失几何细节 24。有向距离场（Signed Distance Fields, SDF）：一种基于栅格的表示，其中每个单元存储到最近障碍物的最小距离。符号表示该单元是在障碍物内部（负值）还是外部（正值）。其变体包括欧几里得有向距离场（ESDF）和截断有向距离场（TSDF）24。优点：碰撞检测极其快速（只需一次查找）。距离值提供了一个梯度，既能指示穿透深度，又能指示通往最近自由空间的方向，这对于基于优化的规划器来说是无价之宝 26。缺点：初始构建成本可能很高，不太适合需要频繁更新距离场的动态环境。碰撞检测策略离散检测：最常见的方法。规划出的路径被离散化为一系列路径点，在每个点上检查机器人的位形是否发生碰撞。如果分辨率过低，可能会错过路径点之间的碰撞。连续碰撞检测：更先进的方法，检查机器人在两个位形之间运动时所扫过的整个体积，从而提供更强的安全保证。基于模型的检测 vs. 无模型的检测：以上方法侧重于先验的几何检查，但机器人也可以在执行期间检测碰撞。基于模型：比较（来自机器人动力学模型的）预期电机扭矩与测量的扭矩。一个急剧且意外的偏差表明发生了碰撞 28。无模型/基于振动：使用加速度计等传感器来检测由撞击产生的独特振动特征，这种方法独立于机器人的动力学模型 30。环境表示的选择与规划算法的选择并非相互独立，它们之间存在着深刻的共生关系。某些算法类别的兴起，得益于特定表示方法的发展，反之亦然。早期的规划器通常使用精确的几何模型（如多边形、网格），碰撞检测涉及复杂的几何相交测试，虽然精确但速度缓慢 31。基于采样的规划器（如 PRM/RRT）需要执行成千上万次碰撞检测，几何测试的缓慢成为了主要瓶颈。这促使了更简单、更快速的表示方法（如占据栅格图）的使用，即使这会牺牲一些精度 24。然而，像 CHOMP 这样的基于优化的规划器，不仅需要一个二元的碰撞信号，还需要一个梯度——即关于如何改变轨迹以减少碰撞的信息。占据栅格图无法提供这样的梯度。正是这种对梯度的特定需求，推动了有向距离场（SDF）的广泛应用。SDF 中的距离值构成了一个完美的、预先计算好的梯度场，它能精确地告诉优化器应该朝哪个方向“推动”轨迹以移出碰撞区域 26。CHOMP 的成功与 SDF 的高效性密不可分。近年来，基于学习的方法正转向隐式表示。一个神经网络不再构建显式地图，而是学习一个函数，将传感器输入（如点云）和位形映射到碰撞概率或有向距离 25。这完全避免了构建显式地图的需要。因此，我们不能简单地替换规划器或表示方法而不考虑其后果。最有效的系统是协同设计算法及其操作的数据结构。复杂场景中规划的未来，可能更多地取决于开发高效的隐式表示，而不是优化显式的表示。第二部分：经典工具箱：基础规划算法4. 基于采样的规划器：探索 C-space动机“维度灾难”使得对于拥有多个自由度的机器人来说，显式构建和搜索 $C_{free}$ 变得不可行。基于采样的规划器通过在 C-space 中进行随机采样来巧妙地回避了这个问题，它们连接有效的采样点以构建自由空间的图（或“路线图”），而无需计算其精确边界 32。概率路线图（Probabilistic Roadmaps, PRM）这是一种多查询方法，非常适合于需要执行多次规划任务的静态环境。阶段一：学习/构建在 C-space 中随机生成大量位形（节点）。丢弃任何处于碰撞状态的节点。对于每个有效节点，找到其 $k$ 个最近的邻居。尝试使用一个简单的、直线的“局部规划器”连接每个节点与其邻居。如果局部路径无碰撞，则在路线图（一个图结构）中添加一条边 11。阶段二：查询对于给定的 $q_{start}$ 和 $q_{goal}$，将它们连接到预先计算好的路线图中的最近节点。使用标准的图搜索算法（如 A* 或 Dijkstra）在路线图上找到起点和终点连接点之间的最短路径 11。优点：对于多次查询极为高效，因为耗时的路线图构建过程只需执行一次 11。缺点：难以找到通过 C-space 中狭窄通道的路径，因为在这样的通道内随机采到一个点的概率非常低 34。快速扩展随机树（Rapidly-exploring Random Trees, RRT）这是一种单查询方法，在快速探索广阔、开放的空间方面非常有效。算法流程初始化一棵以 $q_{start}$ 为根节点的树。循环执行：a. 在 C-space 中选择一个随机位形 $q_{rand}$。b. 在现有树中找到离 $q_{rand}$ 最近的节点 $q_{near}$。c. 从 $q_{near}$ 朝着 $q_{rand}$ 的方向“前进”一小步，生成新位形 $q_{new}$。d. 如果从 $q_{near}$ 到 $q_{new}$ 的运动是无碰撞的，则将 $q_{new}$ 作为新节点添加到树中，并与 $q_{near}$ 连接。重复此过程，直到添加的某个节点足够接近 $q_{goal}$ 11。优点：能快速找到一个解。其树状结构具有向空间未探索区域扩展的内在偏好，使其非常高效 11。缺点：找到的第一个路径通常不平滑且远非最优。它不是多查询算法；对于新的查询，必须重新构建整棵树 11。关键概念与保证概率完备性（Probabilistic Completeness）：这是 PRM 和 RRT 的一个关键特性。它保证如果存在一条路径，随着采样点数量趋于无穷，算法找到该路径的概率将趋近于 1。它用实践中的效率换取了绝对的完备性 11。渐进最优性（Asymptotic Optimality）与 RRT*：RRT 的非最优性催生了像 RRT* 这样的变体。RRT* 在 RRT 的基础上增加了两个关键步骤：在新节点附近“重连”树，如果通过新节点可以提供一条从根节点出发的更短路径，则更新连接；同时检查新节点是否能为其邻居提供更短的路径。随着采样点数量的增加，RRT* 找到的路径保证会收敛到真正的最优路径。当然，这是以更高的计算时间为代价的 12。表 1：经典基于采样规划器的比较特性概率路线图 (PRM)快速扩展随机树 (RRT)查询类型多查询 11单查询 11数据结构无向图 (“路线图”) 33有向树 11探索方式均匀采样整个空间偏向于未探索区域路径质量由于图搜索，质量通常较高（更短）找到的第一个路径通常是次优的 11主要用途静态环境，重复性规划任务动态环境，一次性规划任务核心优势将计算成本分摊到多次查询中快速找到初始可行路径核心劣势难以处理狭窄通道 34路径质量差；新查询需重新运行 11第三部分：前沿技术：现代规划范式5. 基于优化的规划器：将路径优化至最优范式转变基于采样的规划方法擅长在复杂空间中寻找可行路径，但它们主要将问题视为一个几何搜索问题。而基于优化的规划器则将运动规划重新定义为一个数值优化问题：寻找一条能够最小化某个成本函数并满足一系列约束的轨迹 9。这种方法允许对路径质量、平滑度和动力学进行显式推理。它们通常从一个初始的“猜测”轨迹（甚至可能处于碰撞状态）开始，然后迭代地对其进行优化 26。CHOMP (Covariant Hamiltonian Optimization for Motion Planning)核心原理：使用函数梯度下降法来优化一条轨迹。其关键思想是将整条轨迹视为一个无限维空间中的单一点，并计算成本函数相对于整条轨迹的“梯度” 15。目标函数：成本函数是两个项的加权和：$f_{obstacle}$：一个障碍物成本项，用于惩罚与障碍物的接近程度，通常使用预先计算的 SDF 高效地计算 26。$f_{smooth}$：一个平滑度成本项，通常基于最小化轨迹上的速度或加速度的平方 26。优点：能够生成平滑、高质量的轨迹。通过将一条简单的直线路径变形以避开碰撞，通常可以非常快速地解决简单问题 26。缺点：依赖于梯度，这意味着成本函数必须是可微的。容易陷入局部最优，尤其是在复杂、拥挤的环境中 15。其性能在很大程度上取决于初始猜测的质量 15。STOMP (Stochastic Trajectory Optimization for Motion Planning)核心原理：一种无梯度的优化方法。STOMP 不计算梯度，而是通过在当前轨迹周围生成一团带噪声的“探索”轨迹来迭代地探索空间。它评估每条噪声轨迹的成本，并将它们进行加权平均，从而为下一次迭代生成一条更新的、成本更低的轨迹 13。随机性的优势：无梯度：由于不需要导数，STOMP 可以优化任意的、不可微的成本函数（例如，电机扭矩、二元约束）13。逃离局部最优：探索过程的内在随机性使得 STOMP 能够“跳出”那些会困住像 CHOMP 这样的基于梯度的方法的局部最优解 13。优点：相比 CHOMP，对局部最优更具鲁棒性。成本函数设计灵活。缺点：如果存在良好的梯度，其收敛速度可能比基于梯度的方法慢。性能可能依赖于噪声参数的调整。TrajOpt (Trajectory Optimization for Motion Planning)核心原理：将运动规划问题构建为一个约束优化问题，并使用序列二次规划（Sequential Quadratic Programming, SQP）来求解。TrajOpt 并不将碰撞避免和其他目标合并到一个单一的成本函数中，而是将碰撞避免视为一个硬约束，同时优化其他目标，如路径长度或平滑度 16。关键区别：碰撞处理：使用精确的、连续时间的凸体-凸体碰撞检测。这比 CHOMP 和 STOMP 使用的基于点的 SDF 查找更为精确，能更好地处理复杂的几何形状 16。优化方法：SQP 通常比简单的梯度下降法更强大，能更好地处理深度不可行的初始轨迹（即与有效解相距甚远的路径）27。优点：精度高，非常擅长处理复杂约束，并且对较差的初始化具有鲁棒性 27。缺点：凸体形状的假设可能需要将复杂的机器人连杆或障碍物分解为多个凸体部分。每次迭代的计算量可能比 CHOMP 或 STOMP 更大。表 2：基于优化规划器的比较分析特性CHOMPSTOMPTrajOpt核心方法协变梯度下降 26随机优化 13序列二次规划 (SQP) 16梯度需求是 (函数梯度) 26否 (无梯度) 14是 (用于约束/目标)碰撞处理通过 SDF 在成本函数中加入惩罚项 26通过 SDF 在成本函数中加入惩罚项 13通过连续的凸体-凸体检测作为硬约束 27局部最优容易陷入 15因随机性而更具鲁棒性 13可能陷入，但 SQP 通常比梯度下降更鲁棒初始路径需“猜测”，性能对其敏感 15可从不可行路径开始 14对深度不可行的初始路径鲁棒 27最适用场景快速局部优化，梯度良好的问题存在不可微成本、需逃离局部最优的问题具有复杂约束和高精度要求的问题6. 基于学习的规划器：利用数据与经验动机经典规划器是“盲目地”或仅使用简单启发式进行探索。在许多现实世界场景中，机器人会重复执行相似的任务。基于学习的方法旨在利用过去规划经验的数据，来显著加速未来的规划查询 36。其核心思想是学习一个关于在给定环境中“好的解决方案是什么样子”的先验知识。混合方法：学习引导经典规划器MPNet (Motion Planning Network)：这是一个利用神经网络辅助经典规划器的杰出例子。MPNet 是一个被训练来充当智能采样器的神经网络。给定当前状态、目标状态和环境表示（例如，点云），它能直接输出一个有希望的下一个状态。这个输出随后被用来引导像 RRT 这样的基于采样的规划器的探索过程，有效地用“有信息的”或“有偏见的”采样取代了随机采样 39。这种方法结合了 RRT 的探索保证和学习模型的数据驱动效率，将搜索集中在可能包含最优路径的区域 36。学习以“热启动”优化：另一个强大的混合方法是使用神经网络为基于优化的规划器生成一个高质量的初始轨迹。这有助于优化器更快地收敛并避免陷入不良的局部最优 10。端到端学习与 VLA 范式一个更激进的转变是用一个单一的、大型的神经网络取代整个经典规划流程。视觉-语言-行为（Vision-Language-Action, VLA）模型：这些是大型基础模型，通常基于 Transformer 架构，它们接收多模态输入（例如，相机图像、自然语言指令），并直接输出机器人行为 40。案例研究：π₀ (pi-zero)：一个体现了这种范式的最先进的 VLA 模型。架构：它使用一个预训练的视觉-语言模型（VLM）作为其骨干。这使得模型能够从互联网规模的数据中继承大量关于物体、语言和世界的语义知识 41。行为生成：它使用一种称为**流匹配（flow matching）**的技术（一种受扩散模型启发的方法）来生成平滑、连续、高频的未来行为“片段”，这对于灵巧操作至关重要 42。通用策略：通过在来自多种不同机器人类型和任务的大规模、多样化数据集上进行训练，π₀ 学习到一个通用策略，通常能够以零样本（zero-shot）的方式在新环境中执行新任务，只需给定新的图像和语言指令即可 41。运动规划中的“学习”并非单一类别，而是呈现出一个从浅层到深层的融合谱系。最简单的应用是用学习模型替换经典流程中的某个低效组件。例如，使用学习模型来偏置 RRT 中的采样（如 MPNet 39），或为 TrajOpt 提供更好的初始猜测 10。这可以被称为“学习辅助的经典规划”，它保留了经典方法的结构和保证，但使其速度更快。更深层次的融合涉及学习一个反应式策略，将传感器输入直接映射到局部行为，通常使用强化学习（RL）37。这更像是一个“局部规划器”或控制器，而非全局规划器。最深层次的融合是端到端的 VLA 模型（如 π₀ 42）。在这种模型中，没有显式的几何规划器。C-space、碰撞检测和路径搜索等概念不是被编程实现的，而是网络从海量数据中隐式学习到的涌现特性。模型不是在 C-space 中规划一条路径，而是生成一种能够完成语义目标（semantic goal）的行为。这个领域正在出现分化。一条路径是利用有针对性的学习组件来改进经典方法，以提高在明确定义的几何规划问题上的性能。另一条路径，受大型基础模型成功的推动，旨在完全绕过几何问题，直接学习用于完成语义任务的感知-运动行为。未来很可能会是两者的结合：使用 VLA 进行高层语义指导，同时使用经典或优化规划器来确保底层的安全性、精确性和约束满足。第四部分：更广阔的背景与未来展望7. 超越 A 到 B：任务与运动规划（TAMP）定义 TAMP 问题到目前为止讨论的大多数算法解决的都是“从 A 到 B”的运动规划问题。然而，现实世界中的操作要复杂得多。它不仅涉及移动，还涉及与物体互动以实现一个高层目标（例如，“煮一杯咖啡”）。任务与运动规划（Task and Motion Planning, TAMP）就是这样一个领域，它将高层的、符号化的 AI 任务规划（决定动作序列，如 拾取(杯子)、放置(杯子, 咖啡机)、按下(按钮)）与底层的、连续的运动规划（为每个动作计算实际的轨迹）相结合 4。混合搜索空间TAMP 极其困难，因为它需要在混合空间中进行搜索。规划器必须同时找到一个有效的离散动作序列和使该序列在几何上可行的连续参数（例如，抓取姿态、放置位置、无碰撞路径）4。相互依赖问题关键的挑战在于，任务层面的决策与运动层面的可行性是紧密耦合的。一个看似很好的任务计划在几何上可能是不可能的（例如，你无法放置杯子，因为空间被挡住了）。一个 TAMP 求解器必须能够推理这种相互依赖性，允许底层的运动失败来反过来指导高层任务计划的改变 44。TAMP 框架基于约束与基于采样：这些方法通常将问题描述为一组目标条件，并使用约束满足或采样来寻找一个有效的动作和运动序列 4。基于优化的 TAMP：这种现代方法将整个 TAMP 问题构建为一个大型的混合优化问题，并定义一个关于最终计划质量的目标函数。这允许更具表达力的目标（例如，“建造尽可能高的稳定塔”），并通过轨迹优化自然地融入了机器人动力学 4。8. 前沿、挑战与未来方向在动态环境中规划大多数经典规划都假设世界是静态的。一个主要的前沿领域是在有移动障碍物或人类协作者的环境中进行规划。这需要实时的重规划能力。像 Temporal-PRM（T-PRM）这样的算法通过引入时间维度来扩展经典规划器，使其能够找到“等待”动态障碍物通过的路径 1。基于模型 vs. 无模型的权衡基于模型：依赖于机器人和环境的显式模型的方法（如大多数经典和优化规划器）非常高效和精确，但当模型不准确或世界发生意外变化时可能会失败 3。无模型：直接从交互中学习的方法（如许多 RL 和端到端方法）对模型不确定性更具鲁棒性，但通常需要大量数据，并且可能更慢、精度更低 3。未来：未来在于结合这两种方法：在可能的情况下使用模型进行高效规划，但在处理不确定性和意外事件时，回退到学习到的、无模型的策略。基准测试的关键作用随着规划器数量和复杂性的增长，严格、标准化的比较对于科学进步至关重要。框架：像 OMPL 和 MoveIt 这样的工具提供了标准化的软件套件，用于实现和比较规划器 48。像 MotionBenchMaker 这样的框架则帮助生成多样化且具有挑战性的问题集 5。指标：有意义的比较需要一套全面的指标，包括：规划时间、成功率、路径长度、路径平滑度以及路径与障碍物的间隙 5。展望：协同与抽象的未来混合是关键：没有一种单一的规划范式是万能的。运动规划的未来是混合的。我们将看到更深层次的融合：利用采样进行全局探索，利用优化进行局部精化和强制约束，利用学习为两者提供智能的启发式和先验知识 10。学习抽象的兴起：像 π₀ 这样的 VLA 模型的成功，指向了一个在更高抽象层次上进行规划的未来。我们将不再在关节空间中规划路径，而是规划语义技能（打开抽屉、抓取杯子）的序列。这些技能的底层运动生成将由强大的、预训练的生成模型处理，而经典优化器则确保安全性和精确性。这将使机器人技术更容易普及，并使其能够解决非结构化人类世界中真正长时程、复杂的任务 4。